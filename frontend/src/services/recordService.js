// Servicio de historial clínico (frontend) — Medical record service (frontend)
// Se comunica con el backend de InsForge — Communicates with the InsForge backend

const BASE_URL = '/api/records';

// Crea una entrada en el historial clínico — Creates a medical record entry
export async function createEntry(patientId, entryData) {
  // TODO Etapa 1 — implementar lógica / implement logic
}

// Obtiene el historial completo de un paciente — Fetches a patient's full medical history
export async function getPatientHistory(patientId) {
  // TODO Etapa 1 — implementar lógica / implement logic
}

// Ordena entradas del historial por fecha — Sorts history entries by date
export function sortByDate(entries) {
  // TODO Etapa 1 — implementar lógica / implement logic
}
