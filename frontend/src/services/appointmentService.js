// Servicio de citas (frontend) — Appointment service (frontend)
// Se comunica con el backend de InsForge — Communicates with the InsForge backend

const BASE_URL = '/api/appointments';

// Obtiene todas las citas — Fetches all appointments
export async function getAppointments() {
  // TODO Etapa 1 — implementar lógica / implement logic
}

// Crea una nueva cita — Creates a new appointment
export async function createAppointment(appointmentData) {
  // TODO Etapa 1 — implementar lógica / implement logic
}

// Cancela una cita por ID — Cancels an appointment by ID
export async function cancelAppointment(id) {
  // TODO Etapa 1 — implementar lógica / implement logic
}

// Marca una cita como atendida — Marks an appointment as attended
export async function markAsAttended(id) {
  // TODO Etapa 1 — implementar lógica / implement logic
}

// Filtra citas por fecha — Filters appointments by date
export async function filterByDate(date) {
  // TODO Etapa 1 — implementar lógica / implement logic
}
